"""集中管理遊戲尺寸、時間、地圖配置與視覺常數。"""

from __future__ import annotations

from .models import Vector2


WINDOW_WIDTH = 1280
WINDOW_HEIGHT = 720
WORLD_WIDTH = 2400
WORLD_HEIGHT = 1400
FPS = 60
MAX_DELTA_TIME = 0.05

MATCH_DURATION = 240.0
EXTRACTION_START_TIME = 210.0
EXTRACTION_REQUIRED_TIME = 10.0
RESPAWN_DELAY = 5.0
MAX_UPGRADE_STACKS = 10
UPGRADE_PER_STACK = 0.03
MAX_ULTIMATE_ENERGY = 100.0

PLAYER_COUNT = 6
PLAYER_RADIUS = 18.0
PLAYER_BASE_HEALTH = 100.0
PLAYER_BASE_SPEED = 220.0

MONSTER_CAMP_COUNT = 4
MONSTERS_PER_CAMP = 3
MONSTER_RADIUS = 16.0
MONSTER_HEALTH = 85.0
MONSTER_SPEED = 95.0
MONSTER_CONTACT_DAMAGE = 12.0
MONSTER_ATTACK_INTERVAL = 0.8
MONSTER_RESPAWN_DELAY = 6.0

# 飛行物碰撞半徑與瞄準預覽色集中管理；速度由角色定義保存。
BREACH_CONE_ANGLE_DEGREES = 60.0
BREACH_CONE_RANGE = 200.0
BREACH_PELLET_COUNT = 5
BREACH_PROJECTILE_SPEED = 900.0
SNIPER_PROJECTILE_SPEED = 1400.0
HUNTER_PROJECTILE_SPEED = 520.0
MINE_PROJECTILE_SPEED = 650.0
GEOMETRY_EPSILON = 1e-6
BREACH_PELLET_RADIUS = 6.0
SNIPER_PROJECTILE_RADIUS = 8.0
BOOMERANG_PROJECTILE_RADIUS = 12.0
MINE_PROJECTILE_RADIUS = 10.0
AIM_GUIDE_COLOR = (112, 235, 201)
AIM_GUIDE_SECONDARY_COLOR = (158, 217, 255)
AIM_GUIDE_INVALID_COLOR = (129, 132, 142)
AIM_GUIDE_OUTLINE_COLOR = (235, 240, 242)

# 技能圖形使用固定色彩，讓不同角色即使在沒有素材時也能快速辨識。
ABILITY_COLORS = {
    "breach": (255, 174, 74),
    "sniper": (158, 217, 255),
    "guardian": (235, 240, 242),
    "hunter": (173, 133, 255),
    "controller": (255, 196, 92),
    "siphoner": (119, 194, 255),
    "tactical": (89, 213, 176),
}

GUI_OPACITY_PERCENT = 78
GUI_OPACITY_MIN_PERCENT = 50
GUI_OPACITY_MAX_PERCENT = 90


def clamp_gui_opacity_percent(value: int | float = GUI_OPACITY_PERCENT) -> int:
    """將 GUI 背景不透明度限制在 50～90%，保留半透明可讀性。"""

    try:
        numeric_value = float(value)
    except (TypeError, ValueError):
        numeric_value = float(GUI_OPACITY_PERCENT)
    return round(max(GUI_OPACITY_MIN_PERCENT, min(GUI_OPACITY_MAX_PERCENT, numeric_value)))


def gui_panel_alpha(value: int | float = GUI_OPACITY_PERCENT) -> int:
    """將百分比設定轉成 Pygame SRCALPHA 使用的 0～255 alpha。"""

    return round(255 * clamp_gui_opacity_percent(value) / 100)

EXTRACTION_CENTER = Vector2(WORLD_WIDTH / 2, WORLD_HEIGHT / 2)
EXTRACTION_RADIUS = 140.0

# 六個出生點刻意放在世界外圍，讓開場不會把所有玩家重疊在中央。
SPAWN_POINTS = (
    # 人類玩家出生點避開左上角 HUD，避免鏡頭在世界邊界時被介面蓋住。
    Vector2(500, 260),
    Vector2(WORLD_WIDTH / 2, 120),
    Vector2(WORLD_WIDTH - 170, 170),
    Vector2(170, WORLD_HEIGHT - 170),
    Vector2(WORLD_WIDTH / 2, WORLD_HEIGHT - 120),
    Vector2(WORLD_WIDTH - 170, WORLD_HEIGHT - 170),
)

# 怪物區分布在外圍與中間區域，讓玩家有多個發育方向但仍會自然碰面。
MONSTER_CAMP_POINTS = (
    Vector2(560, 350),
    Vector2(1840, 350),
    Vector2(560, 1050),
    Vector2(1840, 1050),
)

BACKGROUND_COLOR = (22, 27, 36)
GROUND_COLOR = (35, 48, 54)
GRID_COLOR = (48, 63, 67)
PANEL_COLOR = (18, 22, 30)
PANEL_BORDER_COLOR = (93, 112, 126)
TEXT_COLOR = (235, 240, 242)
MUTED_TEXT_COLOR = (167, 181, 188)
ACCENT_COLOR = (89, 213, 176)
WARNING_COLOR = (255, 196, 92)
DANGER_COLOR = (245, 105, 103)
EXTRACTION_COLOR = (119, 194, 255)
MONSTER_COLOR = (205, 106, 88)
PLAYER_COLORS = (
    (91, 214, 179),
    (255, 120, 110),
    (255, 196, 92),
    (173, 133, 255),
    (103, 183, 255),
    (244, 125, 196),
)

GRID_SIZE = 100
PLAYER_DRAW_RADIUS = 15
MONSTER_DRAW_RADIUS = 13
